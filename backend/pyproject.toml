[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.13,<3.14"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "openai",
    "python-dotenv",
    "python-multipart",
    "pillow",
    "transformers",
    "torch",
]